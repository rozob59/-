import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gobdha.library',
  appName: 'Dokkhin Gobdha Public Library',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
